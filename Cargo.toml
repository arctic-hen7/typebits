[package]
name = "typebits"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { version = "2", default-features = false, optional = true }

[features]
default = [ "std", "array" ]

std = [ "alloc", "thiserror/std" ]
alloc = []
array = [ "thiserror" ]
